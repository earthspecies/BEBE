save_latents : false
predict_and_evaluate : true
unsupervised : true
  
default_model_config:
  landmark_hop_size : 10
  n_epochs : 10
  n_landmarks_max : 8
  embed_length : 10
  boundary_init_lambda : 4.
  max_track_len : 10000
  batch_size : 10
  time_power_term : 1.
  ## 1 is standard. if between 0 and 1, it penalizes discovering short segments

